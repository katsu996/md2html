# 実装完了報告・レビュー引継ぎ票

> この文書は実装担当者が作業中から更新し、I-11で完成させる。
> `{入力}` を実値へ置換し、該当しない項目は「該当なし」と理由を書く。
> 実行していない検証をpassとして記録しない。

## 1. ステータス

| 項目 | 記入 |
| --- | --- |
| 実装状態 | `{入力: 完了 / 一部完了 / 未完了}` |
| 実装担当 | `GPT-5.6 Terra` |
| 作業開始日 | `{入力: YYYY-MM-DD}` |
| 最終確認日 | `{入力: YYYY-MM-DD}` |
| 基準revision | `{入力: SHAまたは「初期commitなし」}` |
| review対象revision | `{入力: SHAまたはworking tree}` |
| commit / push状態 | `{入力}` |

### 完了していない場合

`{入力: 残作業、停止理由、再開に必要な条件。完了なら「該当なし」}`

## 2. 実装要約

`{入力: 何を実装し、利用者に何が提供されるかを5～10行で記載}`

## 3. 実装環境

| 項目 | 値 |
| --- | --- |
| OS | `{入力}` |
| Node.js | `{入力}` |
| npm | `{入力}` |
| TypeScript | `{入力}` |
| marked | `{入力}` |
| tsdown | `{入力}` |
| Vitest | `{入力}` |

## 4. タスク完了状況

| ID | 状態 | 主な成果物 | 補足 |
| --- | --- | --- | --- |
| I-00 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-01 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-02 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-03 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-04 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-05 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-06 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-07 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-08 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-09 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-10 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |
| I-11 | `{未着手/進行中/完了}` | `{入力}` | `{入力}` |

## 5. 変更ファイル一覧

レビュー対象の手書きファイルをすべて記載する。lockfileは独立行にし、生成された `dist` はgit管理対象でなければ記載不要。

| ファイル | タスク | 変更目的 | リスク |
| --- | --- | --- | --- |
| `{入力}` | `{I-xx}` | `{入力}` | `{低/中/高: 理由}` |

## 6. 最終公開API

### exports

```typescript
{入力: src/index.tsの最終的な公開export一覧}
```

### 型とsignature

```typescript
{入力: ConvertOptions、convertMdToHtml、HtmlDocument、error型の最終signature}
```

### 基本設計との差

`{入力: なし / 差異IDを列挙}`

## 7. 最終CLI仕様

### help出力

```text
{入力: ビルド済みCLIの --help 出力}
```

### 動作確認例

| ケース | 実行コマンド | 結果 |
| --- | --- | --- |
| ファイル変換 | `{入力}` | `{入力}` |
| CSS指定 | `{入力}` | `{入力}` |
| stdin/stdout | `{入力}` | `{入力}` |
| 既存出力拒否 | `{入力}` | `{入力}` |
| force上書き | `{入力}` | `{入力}` |

## 8. 設計差異・実装判断

基本設計と異なる点だけでなく、レビュー判断に役立つ重要な実装方式も記録する。差異がなければ「設計差異なし」とし、重要判断は残す。

| ID | 設計箇所 | 設計または課題 | 実装内容 | 理由 | 互換性・リスク |
| --- | --- | --- | --- | --- | --- |
| `D-01` | `{入力}` | `{入力}` | `{入力}` | `{入力}` | `{入力}` |

## 9. セキュリティ実装

| 境界・脅威 | 防御 | 主な実装箇所 | 検証test |
| --- | --- | --- | --- |
| Markdown生HTML | `{入力}` | `{入力}` | `{入力}` |
| link URL | `{入力}` | `{入力}` | `{入力}` |
| image URL | `{入力}` | `{入力}` | `{入力}` |
| title | `{入力}` | `{入力}` | `{入力}` |
| lang属性 | `{入力}` | `{入力}` | `{入力}` |
| `</style>`注入 | `{入力}` | `{入力}` | `{入力}` |
| 入力Markdown上書き | `{入力}` | `{入力}` | `{入力}` |
| 入力CSS上書き | `{入力}` | `{入力}` | `{入力}` |
| 一時ファイル競合・残存 | `{入力}` | `{入力}` | `{入力}` |

### `rawHtml: "allow"` の制約

`{入力: allow時に何が許可され、何を安全化しないか}`

## 10. 検証結果

同じ最終状態に対して実行した結果を書く。長いログ全文ではなく、exit code、test数、coverage、警告数等を記載する。

| コマンド | 状態 | 結果要約 |
| --- | --- | --- |
| `npm ci` | `{pass/fail/not run}` | `{入力}` |
| `npm run typecheck` | `{pass/fail/not run}` | `{入力}` |
| `npm run lint` | `{pass/fail/not run}` | `{入力}` |
| `npm test` | `{pass/fail/not run}` | `{入力: test files / tests / duration}` |
| `npm run test:coverage` | `{pass/fail/not run}` | `{入力: lines / branches / functions}` |
| `npm run build` | `{pass/fail/not run}` | `{入力: 生成ファイル}` |
| `npm run package:check` | `{pass/fail/not run}` | `{入力: publint / attw}` |
| `npm pack --dry-run` | `{pass/fail/not run}` | `{入力: file数 / unpacked size}` |
| `git diff --check` | `{pass/fail/not run}` | `{入力}` |

### 実行しなかった検証

`{入力: コマンド、理由、未検証リスク。すべて実行した場合は「なし」}`

## 11. package smoke test

| 項目 | 結果 | 使用fixture・コマンド |
| --- | --- | --- |
| ESM import | `{pass/fail/not run}` | `{入力}` |
| CJS require | `{pass/fail/not run}` | `{入力}` |
| CLI from tarball | `{pass/fail/not run}` | `{入力}` |
| browser bundle | `{pass/fail/not run}` | `{入力}` |

### pack内容

```text
{入力: npm pack --dry-runのファイル一覧または要約}
```

## 12. 視覚確認

| 項目 | 値 |
| --- | --- |
| 確認用fixture | `{入力}` |
| browser / renderer | `{入力}` |
| デスクトップviewport | `{入力}` |
| モバイルviewport | `{入力}` |
| print確認 | `{入力}` |
| 結果 | `{入力}` |

### 視覚上の既知課題

`{入力: なし / 内容と影響}`

## 13. 対応環境の確認

| 環境 | 状態 | 補足 |
| --- | --- | --- |
| Node.js 22 LTS | `{pass/fail/not run}` | `{入力}` |
| Node.js 24 LTS | `{pass/fail/not run}` | `{入力}` |
| 現行Node.js | `{pass/fail/not run}` | `{入力}` |
| Linux CLI | `{pass/fail/not run}` | `{入力}` |
| Windows CLI | `{pass/fail/not run}` | `{入力}` |

## 14. 既知課題・残作業

| ID | 内容 | 影響 | 回避策 | 1.0.0 blocker |
| --- | --- | --- | --- | --- |
| `K-01` | `{入力}` | `{入力}` | `{入力}` | `{yes/no}` |

課題がない場合は表を削除して「既知課題なし」と記載する。

## 15. 公開前確認事項

| 項目 | 状態 | 必要な判断 |
| --- | --- | --- |
| npmパッケージ名 | `{入力}` | `{入力}` |
| ライセンス | `{入力}` | `{入力}` |
| author | `{入力}` | `{入力}` |
| repository URL | `{入力}` | `{入力}` |
| npm publish | `未実施` | 所有者の明示承認 |

## 16. レビュー重点箇所

Solが優先して確認すべき場所を、ファイルと関数名または行番号で記載する。

| 優先度 | ファイル・場所 | 理由 | 関連test |
| --- | --- | --- | --- |
| `高` | `{入力}` | `{入力}` | `{入力}` |

最低限、URL検証、生HTML、style終端、HTML template、atomic write、exports/typesを含める。

## 17. 再現手順

クリーンなcheckoutまたは現在のworking treeから、レビュー可能な状態にする手順を書く。

```bash
{入力: installから全検証までのコマンド。秘密値や個人固有パスを含めない}
```

## 18. 実装者セルフレビュー

- [ ] 要件定義と基本設計を再読した
- [ ] 公開APIとCLI helpが文書と一致する
- [ ] ライブラリ共通コードにNode.js builtinがない
- [ ] CLIは共通変換コアを利用する
- [ ] default modeのraw HTMLと危険URLをテストした
- [ ] title、lang、style終端を文脈別に安全化した
- [ ] 入力MarkdownとCSSの誤上書きをテストした
- [ ] atomic write失敗時の既存出力保護をテストした
- [ ] ESM/CJSの実行と型解決をtarballから確認した
- [ ] 視覚確認を行った
- [ ] skip、TODO、FIXME、不要な型抑制がない
- [ ] 実行していない検証と既知課題を明記した
- [ ] commit、push、publish状態を正確に記載した
- [ ] 本文にプレースホルダーが残っていない

## 19. レビュー担当への補足

`{入力: 上記に含まれない注意点。なければ「なし」}`

